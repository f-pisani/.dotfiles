#!/bin/bash

alias ..='cd ..'
alias c='clear'
alias ll='ls -lha'
alias gh='history | grep'
alias mkdir='mkdir -p'
alias df='df -H'
alias du='du -ch'
alias mylocalip='hostname -I | awk "{print $1}"'
alias mypublicip='curl ifconfig.me'
